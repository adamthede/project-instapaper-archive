# Core Dependencies (Instapaper export & legacy import)
requests>=2.28.0
requests-oauthlib>=1.3.1
python-dotenv>=0.20.0
markdownify>=0.11.0
beautifulsoup4>=4.11.0
markitdown>=0.0.1a2

# AI Enrichment
google-generativeai>=0.3.0
ollama>=0.1.6
python-frontmatter>=1.0.0

# Controlled vocabulary derivation (scripts/vocab/) — PCA and an exact kNN
# graph over the concept/topic embeddings, then connected components on it.
# numpy and scipy arrive transitively via pandas/scikit-learn, but these
# modules import them directly, so they are declared directly.
scikit-learn>=1.5.0
numpy>=1.24.0
scipy>=1.10.0

# Dashboard & Analytics
streamlit>=1.32.0
pandas>=2.0.0
plotly>=5.18.0
pyarrow>=15.0.0
textstat>=0.7.3
watchdog>=4.0.0
wordcloud>=1.9.0
matplotlib>=3.7.0

PyYAML
